<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Existing invoice / CSV / temporary files -->
    <cache-path
        name="cache_files"
        path="." />

    <!-- Permanent expense receipt photos stored inside the app -->
    <files-path
        name="expense_receipts"
        path="expense_receipts/" />

</paths>
